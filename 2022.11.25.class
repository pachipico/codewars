// Remove the parentheses
// In this kata you are given a string for example:

// "example(unwanted thing)example"
// Your task is to remove everything inside the parentheses as well as the parentheses themselves.

// The example above would return:

// "exampleexample"
// Notes
// Other than parentheses only letters and spaces can occur in the string. Don't worry about other brackets like "[]" and "{}" as these will never appear.
// There can be multiple parentheses.
// The parentheses can be nested.

// 내 답안
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class Kata {

  public static String removeParentheses(final String str) {
    String res = str;
    Pattern pt = Pattern.compile("[(][a-zA-Z0-9 ]{0,}[)]");
    Matcher matcher = pt.matcher(res);
    while (matcher.find()) {
      res = res.replaceAll("[(][a-zA-Z0-9 ]{0,}[)]", "");
    }
    return pt.matcher(res).find() ? removeParentheses(res) : res;
  }
}

// 모범 답안
public class Kata {

  public static String removeParentheses(final String str) {
    String updated = str.replaceAll("\\([^()]*\\)", "");
    if (updated.contains("(")) updated = removeParentheses(updated);
    return updated;
  }
}
